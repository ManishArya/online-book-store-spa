import { AppCommaSeparatorPipe } from './app-comma-separator.pipe';

describe('AppCommaSeparatorPipe', () => {
  it('create an instance', () => {
    const pipe = new AppCommaSeparatorPipe();
    expect(pipe).toBeTruthy();
  });
});
