import { AddressSepratorPipe } from './address-seprator.pipe';

describe('AddressSepratorPipe', () => {
  it('create an instance', () => {
    const pipe = new AddressSepratorPipe();
    expect(pipe).toBeTruthy();
  });
});
